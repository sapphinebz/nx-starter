import { ViewportSizeDirective } from './viewport-size.directive';

describe('ViewportSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportSizeDirective();
    expect(directive).toBeTruthy();
  });
});

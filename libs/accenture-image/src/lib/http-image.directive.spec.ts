import { HttpImageDirective } from './http-image.directive';

describe('HttpImageDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpImageDirective();
    expect(directive).toBeTruthy();
  });
});

import { OnPasteImageDirective } from './on-paste-image.directive';

describe('OnPasteImageDirective', () => {
  it('should create an instance', () => {
    const directive = new OnPasteImageDirective();
    expect(directive).toBeTruthy();
  });
});
